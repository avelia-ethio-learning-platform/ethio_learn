{
  "name": "ethiopialearn",
  "private": true,
  "version": "0.1.0",
  "description": "EthiopiaLearn — educator-first online learning marketplace for Ethiopia (MVP)",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "seed": "pnpm --filter @ethiopialearn/auth-service seed"
  },
  "devDependencies": {
    "turbo": "^2.0.6",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=20"
  }
}
